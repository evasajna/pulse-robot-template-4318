// Temporarily disabled until database types are synchronized
export const addSampleQuestions = async (quizId: string) => {
  console.log("Sample questions functionality disabled until database sync");
  return { success: false, error: "Database not synchronized" };
};